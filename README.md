# Breast_Cancer_detection-USING-XGBOOST-classifier
This project aims to build a machine learning model to classify breast cancer tumors as malignant or benign using the XGBoost (Extreme Gradient Boosting) classifier. The model is trained on the Breast Cancer Wisconsin Diagnostic Dataset from sklearn.datasets, which contains 30 numerical features computed from digitized images of a breast mass .
